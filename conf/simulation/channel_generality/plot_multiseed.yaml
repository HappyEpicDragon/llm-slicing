# @package _global_

defaults:
  - /simulation/channel_generality/_base
  - _self_

mode: plot_multiseed

plot_multiseed:
  scenario: 9
  seeds: [0, 1, 2, 3, 4]
  methods: [ppo_ha_weighted, dt_baseline, dt]
  outdir: ${paths.figures_root}/channel_generality/diagnostic
