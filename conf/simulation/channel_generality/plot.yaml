# @package _global_

defaults:
  - /simulation/channel_generality/_base
  - /simulation/channel_generality/groups/plot_common
  - _self_

mode: plot

plot:
  plot_scenario_list: [5, 6, 7, 8, 9]
  baselines:
    - ppo_baseline
    - ppo_ha_weighted
    - dt
    - dt_baseline