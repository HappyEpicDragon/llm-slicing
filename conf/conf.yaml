defaults:
  - _self_
  - paths: default
  - simulation: null

hydra:
  run:
    # dir: outputs/runs/${now:%Y-%m-%d-%H-%M-%S} 
    dir: outputs/runs/${name}/${mode}/${now:%Y-%m-%d-%H-%M-%S}
  sweep:
    dir: outputs/runs/multirun/${now:%Y-%m-%d-%H-%M-%S}
    subdir: ${hydra.job.num}


