数据全部核实完毕。现在给你一份完整的补测清单：

---

## 补测清单

### 🔴 P0 — 阻塞性缺口（影响论文核心结论）

| # | 方法 | 问题 | 影响 | 补测命令 | 状态 |
|---|---|---|---|---|
| 1 | **M4 PPO-baseline** | metric_raw 只有 **20ep/seed**（5 seeds × 20ep），远低于统一口径 100ep | Fig.B 时序曲线代表性不足，与其他方法口径不一致 | 重跑 `test_ppo_baseline_multi` 令 `n_episodes=100`，写出 raw | ⏸️ 本轮按要求不补测 |
| 2 | **M2 DT-Mix** | metric_raw 只有 **1 seed**，没有 std | Fig.B 和 Fig.A 无误差条，主表缺不确定性估计 | 补跑 `test_dt_baseline` 的 seed 1–4 | ✅ 已补齐（s5-s9 × seed0-4 × 100ep；raw 25/25） |

---

### 🟡 P1 — 质量提升（现在可出图但有短板）

| # | 方法 | 问题 | 影响 | 补测命令 | 状态 |
|---|---|---|---|---|
| 3 | **M5 CQL-Discrete** | **metric_raw 完全缺失**（只有 metric_json） | 若后续想把 M5 纳入 Fig.B 则无法出图 | 重跑 `test_cql_v3` 并在测试脚本中写出 step-level raw | ✅ 已补齐（raw 25/25） |
| 4 | **M6 PPO-Lagrangian-Discrete** | **metric_raw 完全缺失** | 同上；Fig.C 已可用（依赖 nhp_violations.json） | 重跑 `test_ppo_lagrangian` 并写 raw | ✅ 已补齐（raw 25/25） |
| 5 | **M6b PPO-Lagrangian-Mix** | **metric_raw 完全缺失** | 同上 | 重跑 `test_ppo_lagrangian_baseline` 并写 raw | ✅ 已补齐（raw 25/25） |
| 6 | **M6/M6b** summary.json | `n_episodes` 字段缺失（内容 100ep，但无法程序化验证） | 脚本无法自动核查口径 | 补写或在测试脚本里写入该字段 | ✅ 已补齐（测试脚本写入 + 场景/全局 summary） |

---

### 🟢 P2 — 无需补测，现状已满足 Phase 3 要求

| 方法 | 状态 |
|---|---|
| **M1 IDT** | 5 seeds × 100ep × s5–s9，metric_json + metric_raw 齐全 ✅ |
| **M3 PPO-Discrete** | 5 seeds × 100ep × s5–s9，metric_json + metric_raw 齐全 ✅ |
| **M5 CQL-Discrete** | 5 seeds × 100ep × s5–s9，metric_json 齐全（Fig.A/Tab.A）✅ |
| **M5b CQL-Mix** | 5 seeds × 100ep，metric_json + metric_raw 齐全 ✅ |
| **M6 PPO-Lagrangian-Discrete** | 5 seeds × 100ep，metric_json 齐全，Fig.C 专用 json 已有 ✅ |
| **M6b PPO-Lagrangian-Mix** | 5 seeds × 100ep，metric_json 齐全 ✅ |

---

### 当前各图的可出图性

| 图 | 当前状态 | 阻塞原因 |
|---|---|---|
| **Fig.A 柱状图** | ✅ 可出（已出） | — |
| **Tab.A 主表** | ✅ 可出（已出） | — |
| **Fig.C 退化直方图** | ✅ 可出（已出） | — |
| **Fig.B 时序曲线** | ⚠️ 可出但 M4 口径不一致、M2 无误差条 | M4 仅 20ep/seed，M2 单 seed |

**本轮结论**：除 `PPO-baseline` 外，补测项已全部完成并通过 `check_assets.py --require-raw --require-global` 验证。